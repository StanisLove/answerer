%h3= @question.title
%p= @question.body
%br
- @answers.each do |answer|
  %div{class: "answer-#{answer.id}"}
    %h3 Ответ
    - if user_signed_in? && (current_user.id == answer.user_id)
      = link_to 'Удалить ответ',
        answer_path(@question, answer), method: :delete
      |
      = link_to 'Редактировать ответ'
    %p= answer.body
  %br
