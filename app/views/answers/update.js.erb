$('#notice').html('<%= j flash[:notice] %>');
<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |message| %>
    $('.answer-errors').html('<%= j message %>');
  <% end %>
<% else %>
  $('.answer-errors').empty();
  $('#answer-<%= @answer.id %> p').html('<%= j @answer.body %>');
<% end %>
