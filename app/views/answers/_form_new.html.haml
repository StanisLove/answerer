= form_for [@question, @answer], remote: true do |f|
  .field
    = f.label :body, 'Новый ответ'
    .answer-errors
    = f.text_area :body
    = render 'attachments/fields_for', f: f,
      attachments: @answer.attachments.build

  .actions
    = f.submit 'Отправить ответ'
