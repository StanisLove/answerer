%h1 Questions in the last day.

- if @questions.any?
  - @questions.each do |question|
    %p= "#{question.user.email} asked #{l(question.created_at, format: :day)} " + |
        "at #{l(question.created_at, format: :time)}:"                            |
    = link_to "#{question.title}", question
    %br
- else
  %p There are no questions today.