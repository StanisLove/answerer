%h1 Задайте вопрос

= form_for @question do |f|
  .field
    = f.label :title, 'Заголовок'
    = f.text_field :title  
  .field
    = f.label :body, 'Вопрос'
    = f.text_area :body
  .attachments
    = f.fields_for  :attachments do |a|
      = a.label :file, "Файл"
      = a.file_field :file
  .actions
    = f.submit 'Спросить'
