= form_for @question, remote: true do |f|
  .field
    = f.label :title, 'Заголовок'
    = f.text_field :title
  .field
    = f.label :body, 'Вопрос'
    = f.text_area :body
  .actions
    = f.submit 'Сохранить', class: 'edit-question-button'
