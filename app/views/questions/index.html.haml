- @questions.each do |q|
  %div{class: "question-#{q.id}"}
    %h3= q.title
    -if user_signed_in? && (current_user.id == q.user_id)
      = link_to 'Удалить вопрос', q, method: :delete
      |
      = link_to 'Редактировать вопрос'
    %p= q.body
    = link_to 'Написать ответ',
      new_question_answer_path(q.id)
  %br
